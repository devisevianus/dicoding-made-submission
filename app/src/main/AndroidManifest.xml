<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.stickearn.dicodingmadesubmission1">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    
    <permission android:name="com.stickearn.dicodingmadesubmission1.READ_DATABASE" android:protectionLevel="normal" />
    <permission android:name="com.stickearn.dicodingmadesubmission1.WRITE_DATABASE" android:protectionLevel="normal" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning"
        tools:targetApi="m">

        <activity android:name=".view.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.movie.detail.MovieDetailActivity" />
        <activity android:name=".view.tv.detail.TvShowsDetailActivity" />
        <activity android:name=".view.settings.SettingsActivity" />

        <!-- Receiver -->
        <receiver android:name=".util.MovieBannerWidget">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/movie_banner_widget_info" />
        </receiver>

        <receiver android:name=".util.AlarmReceiver"
            android:enabled="true"
            android:exported="true"
            tools:ignore="ExportedReceiver" />

        <receiver android:name=".util.AlarmReceiverV2"
            android:enabled="true"
            android:exported="true"
            tools:ignore="ExportedReceiver" />

        <!-- Service -->
        <service
            android:name=".util.StackWidgetService"
            android:permission="android.permission.BIND_REMOTEVIEWS" />

        <!-- Provider -->
        <provider
            android:name=".helper.MovieProvider"
            android:authorities="com.stickearn.dicodingmadesubmission1"
            android:enabled="true"
            android:exported="true"
            android:readPermission="com.stickearn.dicodingmadesubmission1.READ_DATABASE"
            android:writePermission="com.stickearn.dicodingmadesubmission1.WRITE_DATABASE" />

    </application>

</manifest>